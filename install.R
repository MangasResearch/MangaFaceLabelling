# bibliotecas de interface gráfica

install.packages(c("shiny", "shinyalert", "shinythemes"))

# bibliotecas para carregar imagem e ajustar o texto dinamicamente
# sudo apt install libcairo2-dev
# sudo apt install libtiff-dev

install.packages(c("imager", "glue"))


# biblioteca usada para operações no banco de dados
# sudo apt install libpq-dev

install.packages(c("DBI", "RPostgres"))


